[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pbar"
version = "0.1.0"
description = "シンプルで使いやすいCLIプログレスバーライブラリ"
requires-python = ">=3.8"